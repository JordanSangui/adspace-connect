import { Router } from 'express';
import type { Router as IRouter } from 'express';

const router: IRouter = Router();

// TODO: Implémenter les routes des preuves d'exécution (F-07)

export default router;
